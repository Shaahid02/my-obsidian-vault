|   |   |   |   |   |   |   |   |   |   |   |   |   |   |   |   |   |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
                
|Gap ID|Research Gap|Gap Type|Evidence from Literature|Existing Approach & Its Limitation|Why CSE Exposes It|Contribution to Body of Knowledge|Proposed Direction|Affected System Component|Required Data|Baseline Required|Experiment & Ablation|Novelty Claim|Novelty Rating (1-5)|Feasibility|Risk|Decision|
|G1|Execution friction in a thin, whale-dominated market. Every multi-agent and deep-RL trading framework I've read assumes an order fills at the quoted price. None of them model what happens when your order is a meaningful share of the day's volume.|Methodological + Domain|TradingAgents (P01) trades AAPL/GOOGL/AMZN with no transaction costs or slippage at all. FinVision (P02) the same. AlphaAgents (P04) states a frictionless, zero-slippage environment outright. Kashif & Slepaczuk (P15) do better - 2bps costs plus a turnover penalty - but admit there's no sensitivity check against worse execution. Pippas et al. (P05) make this their sharpest criticism of the whole RL-in-finance field: nearly every paper treats transaction costs as a fixed percentage and assumes certain execution, ignoring varying bid-ask spreads and slippage.|Existing approach: execute the full parent order at the closing price, subtract a flat basis-point fee. Limitation: the cost of trading is treated as a constant when in reality it's a function of your own order size relative to available depth, so the backtest systematically overstates returns and understates drawdown for anything but a tiny position.|CSE is turgid rather than fluid and is dominated by whales - a handful of participants who can move large blocks and shift the price. Dissanayake & Nanayakkara (P11) show illiquidity shocks depress contemporaneous CSE returns significantly (-0.084 to -0.097) and that small caps are more exposed than large (-0.071 vs -0.060), which is direct evidence that liquidity is a first-order effect here, not a rounding error. A flat-fee assumption that is roughly harmless on the Nasdaq is not harmless on the CSE.|Technical: an execution model where transaction cost is a state-dependent function of order size and available liquidity rather than a constant, wired into an RL reward function. Domain: the first quantified account of what execution friction actually costs an automated strategy on the CSE.|Trader agent interfaces with a simulated CSE exchange environment instead of a price series. Parent orders split across intraday intervals in proportion to the historical volume profile, with an Amihud-based illiquidity penalty priced into the fill. Slippage measured in basis points against market VWAP and fed back into the RL reward as a cost term, so the agent learns to avoid orders it can't execute cheaply.|Trader agent; simulated exchange environment; RL reward function.|CSE daily price and turnover per ticker (for the volume profile and the Amihud measure). Intraday data if I can get it - the CSE API exposes a 'today' price feed but historical intraday depth is unconfirmed and may not exist, in which case the volume profile has to be approximated from daily turnover.|(a) The same agent with a flat 2bps cost, matching P15's setup. (b) Zero-cost execution, matching P01/P02/P04. (c) Naive full-size execution at close. The point is to show how much of the reported edge in the frictionless setups is an artefact.|Experiment: run the full framework under all three cost regimes on the same CSE universe and period. Ablation: turn off the order-splitting logic while keeping the illiquidity penalty, and vice versa, to separate the contribution of execution scheduling from the contribution of cost-aware reward shaping. Also test across market-cap tiers, since P11 says small and large caps behave differently.|Nobody has put a liquidity-aware execution model underneath an LLM multi-agent trading framework, and nobody has done it for a frontier market at all.|5|High for the daily-turnover version. Medium-to-low for anything intraday, because historical intraday CSE data may simply not be obtainable - this needs probing in week one.|The intraday volume profile may be unavailable, which would force a cruder daily approximation and weaken the slippage model. There's also no ground-truth execution data to validate the simulator against, so the friction model is a defensible assumption rather than a calibrated fact - I need to be upfront about that rather than presenting simulated slippage as measured slippage.|ACCEPT - core contribution|
|G2|Volatility long memory is measured on the CSE but never used. The fractional integration parameter d sits in a results table instead of doing anything.|Methodological|Riyath (P12) establishes long memory on the CSE with ARMA-FIGARCH across three regimes - the fractional differencing parameter d stays significant in every period for ASPI and is consistently weaker for S&P SL20 - but the study is explicitly descriptive, and no forecasting model is built on the estimates. Samarawickrama & Pallegedara (P13) find persistence (alpha + beta) close to one in every specification and stop there without testing whether that's a unit root, near-unit-root short memory or genuine long memory. On the agent side, TradingAgents' (P01) risk manager assesses volatility qualitatively via an LLM, and AlphaAgents (P04) reduces risk to a prompt-level tolerance profile.|Existing approach: econometricians estimate volatility persistence and publish the coefficients; agent builders ask an LLM whether things look risky. Limitation: the two never meet. The LLM has no grounded second-moment estimate and the econometric model has no decision to feed.|CSE volatility doesn't decay the way developed-market models assume - it has genuine long memory that persists after a shock has technically passed, and it behaves differently on the broad index than on the blue chips. A risk layer calibrated on exponential decay will systematically under-reserve after a shock, which on a market that took the Easter attacks, COVID and the 2022 economic crisis back to back is a serious failure mode.|Technical: using a fractional integration parameter as a live position-sizing constraint inside an autonomous trading system rather than as a reported statistic. Domain: extending Riyath's descriptive finding into a predictive, operational one and testing whether long-memory-aware sizing actually improves risk-adjusted outcomes on the CSE.|A hybrid volatility risk agent: ARMA-FIGARCH(1,d,1) estimated per regime gives the memory parameter, which sets a dynamic exposure cap that decays hyperbolically after a shock rather than snapping back. GJR-GARCH runs alongside it for the asymmetry term, since P13 confirms a significant leverage effect (gamma = 0.103, p = 0.022) - negative news should tighten the cap harder than positive news of the same size loosens it.|Volatility risk agent; position-sizing constraint on the trader agent; RL reward risk penalty.|Daily ASPI and S&P SL20 closes over a window that covers normal, COVID and crisis regimes. Per-ticker daily returns for stock-level volatility. CBSL macro series for regime context.|(a) Static position-sizing cap. (b) Short-memory GARCH(1,1)-based cap, which is what a standard implementation would do. (c) The LLM-only qualitative risk assessment used by P01/P04. Comparing against (b) is the one that proves the long-memory component is earning its place rather than just adding parameters.|Experiment: identical framework, four risk layers, same universe and period, reported across the three regimes separately rather than pooled. Ablation: FIGARCH memory term on/off, leverage asymmetry on/off, and the interaction. Also replicate P12's ASPI vs SL20 split to check whether the cap needs different calibration by index composition. Report in-sample fit and out-of-sample forecast accuracy separately, because P13 found the in-sample winner (GJR-GARCH) and out-of-sample winner (EGARCH) disagree.|First use of a FIGARCH-derived memory parameter as an operational risk guardrail in an agentic trading system, and the first predictive use of CSE long-memory estimates at all.|5|High. The econometrics is well-trodden, the data is daily index closes which I can get, and P12 and P13 between them give me both the specification and the expected parameter ranges to sanity-check against.|Estimating FIGARCH per regime on short subsamples (the COVID window is only two years) risks unstable d estimates. P12's normality assumption is also questionable on fat-tailed returns, so I should test a Student-t specification as a robustness check rather than inheriting that weakness.|ACCEPT - core contribution|
|G3|There is no Sinhala financial-sentiment resource in existence. Every sentiment component in the multi-agent literature is English-only, and every available Sinhala sentiment dataset is from some other domain.|Data + Domain|TradingAgents (P01), FinVision (P02) and AlphaAgents (P04) all run sentiment in English and none use a dedicated sentiment model - the LLM judges tone inline. Zhao et al. (P08) benchmark RAG across 6 LLMs and 15 retrieval conditions, all monolingual English, and explicitly drop chunks that are majority non-English. Rizvi et al. (P10) is the only paper anywhere near the local languages and it targets retail banking customer reviews, not markets. On the dataset side: the Senevirathne TALLIP corpus and its 2025 extension are news and gossip comments, NSINA is unlabelled news that stops in May 2020, SOLD is offensive-language tweets, SalAngaBhava is e-commerce product reviews. None of them are financial.|Existing approach: run an English sentiment model, or ask GPT how a headline feels. Limitation: on the CSE a large share of retail investor discussion is in Sinhala, Singlish or code-mixed text, so an English-only pipeline is simply blind to most of the signal it claims to measure. And a generalist zero-shot model doesn't fix it - P10 shows GPT-4o zero-shot at 81.5% against a fine-tuned XLM-R + lexicon at 88.4%.|CSE retail investors post in Sinhala and Singlish on local forums and social media. Any sentiment agent that only reads English is measuring the wrong population. This isn't a nice-to-have localisation exercise, it's the difference between the sentiment agent having a signal and having noise.|Technical: a financial-domain Sinhala/Singlish/code-mixed sentiment pipeline with an aspect inventory built for equities rather than customer service. Domain: the first annotated Sinhala financial-sentiment dataset, which is a contribution independent of whether the trading framework works.|XLM-RoBERTa backbone, warm-started on the 19,875-comment extended Senevirathne set, then fine-tuned on a CSE-specific set I annotate myself. Swa-Bhasha transliteration to normalise Singlish into Sinhala script before the model sees it. A hand-built market-vocabulary lexicon correction layer on top, since P10 shows that layer alone was worth +10.2% accuracy - more than any model swap. Softmax aggregation across aspects so one extreme aspect doesn't dominate a mixed post. LIME for per-instance explanation.|Sentiment analyst agent; the sentiment feature fed into the RL state and into the research debate.|Local financial news (Daily FT, EconomyNext, Daily Mirror Business, Lanka Business Online - all robots-permitted), CSE announcements, Sri Lankan investor forum posts. Plus the existing Sinhala corpora for warm-start and domain-adaptive pretraining.|(a) English-only sentiment on the same event set, which is what P01/P02/P04 effectively do. (b) GPT-4o zero-shot multilingual. (c) Base XLM-R with no lexicon layer. (d) Turnover as the sentiment proxy, which is what P11 uses and is free.|Experiment: sentiment classification accuracy and macro-F1 on a held-out CSE set, then downstream - does adding the sentiment feature improve the trading agent's risk-adjusted return over the turnover-proxy baseline? Ablation: lexicon layer on/off, transliteration on/off, warm-start on/off. Baseline (d) is the important one, because if a free price-derived proxy does as well as a whole NLP pipeline, that's a finding worth reporting honestly.|No Sinhala financial-sentiment dataset exists. Creating one and putting it inside a trading framework is new on both counts.|5|Medium. The modelling is tractable and the models exist (XLM-R, SinBERT, the fine-tuned sinhala-nlp checkpoints), but annotation is manual labour and the realistic accuracy ceiling is low - the 2025 state of the art on 4-class Sinhala sentiment is about 66% accuracy / 62% macro-F1. That has to be designed around, not wished away.|Annotation effort could eat the schedule. Licensing is a real problem - the TALLIP dataset, SOLD and Swa-Bhasha all have no stated licence, and P10's banking dataset isn't stated to be public. News text is copyrighted even where robots.txt permits crawling, so I can publish derived features but not raw articles. Mitigation: build the sentiment agent to emit calibrated confidence so the orchestrator can down-weight it when it's unsure, rather than pretending it's reliable.|ACCEPT - core contribution|
|G4|Structure-aware and multimodal financial RAG has only ever been tested on SEC filings. CSE annual reports are a different document population and nobody has checked whether any of it transfers.|Technical + Domain|FinanceBench (P06) is 361 US filings and shows GPT-4-Turbo with retrieval failing or refusing 81% of questions. Jimeno Yepes et al. (P07) get their best retrieval scores (84.4% page level) by combining chunking strategies, with element-based chunking specifically winning on paragraph-level accuracy and efficiency - all measured on FinanceBench. MultiFinRAG (P09) gets 75.3% overall with modality-aware indexing, on SEC EDGAR. AlphaAgents (P04) uses section-based chunking, on 10-Ks. Every single one of these is SEC-formatted, English, and XBRL-adjacent.|Existing approach: chunk on document structure, detect tables with a model trained on SEC-like layouts, retrieve per modality. Limitation: all of it leans on the regularity of SEC filings. P07 is explicit that the method depends on parse quality and that scanned or legacy PDFs with OCR segmentation errors break the elements.|CSE hosts text-native annual and quarterly report PDFs for its listed companies, which is good, but there's no XBRL, no standard schema, and report structure varies a lot company to company. Older filings are more likely to be scanned. So the structural assumptions that make element-based chunking work on 10-Ks are exactly the assumptions that need re-testing here.|Technical: an element-based, modality-aware RAG pipeline validated on a document population it wasn't designed for, with the failure modes documented. Domain: the first financial QA evaluation set built on CSE filings, which doesn't currently exist in any form.|Element-based chunking (title starts a new chunk, table never split, merge elements under the length threshold) as the base, plus MultiFinRAG's modality-specific similarity thresholds and tiered fallback for the table-heavy sections. Citation-enforced generation with an explicit 'insufficient information' instruction, because P08 shows pseudo-helpfulness is the default failure when the gold chunk is missing. Re-rank so the gold chunk lands at position 1 and keep top-k low, since P08 shows top-20 is worse than top-3.|Fundamental analyst agent (ingestion and retrieval layers).|CSE-listed company annual and quarterly report PDFs, harvested via the CSE announcement endpoints from the report CDN. Plus a hand-built CSE question/answer/evidence set for evaluation, following FinanceBench's triplet format.|(a) Fixed 512-token chunking, which P07 found is the closest naive baseline. (b) Flat text-only RAG with tables flattened, which P09's baseline shows scores near zero on table questions. (c) Long-context - feed the whole filing, no retrieval.|Experiment: build a CSE evaluation set in FinanceBench triplet format (question, answer, evidence string, page number) and score page-level retrieval, paragraph-level ROUGE/BLEU and end-to-end QA accuracy using the correct / incorrect / failure-to-answer taxonomy. Ablation: element-based vs fixed chunking, modality thresholds on/off, re-ranking on/off, and specifically a scanned-vs-text-native split to quantify the OCR degradation P07 warns about.|First structure-aware multimodal RAG evaluation on non-SEC frontier-market filings, and the first CSE financial QA evaluation set.|4|High. The report PDFs are free and text-native, the chunking libraries are open, and the method is well documented. The cost is building the evaluation set by hand, which is slow but not risky.|Discovery is awkward - report filenames on the CSE CDN are opaque ID/timestamp pairs, so links have to be harvested from the announcement endpoints rather than enumerated. CSE's terms of use for data reuse are unconfirmed and need to be resolved in writing before anything derived gets published. And the evaluation set will be small, so results will be indicative rather than definitive.|ACCEPT - supporting contribution|
|G5|Sentiment-return relationships validated on developed markets don't transfer, and the multi-agent literature assumes they do.|Contextual / Theoretical|Dissanayake & Nanayakkara (P11) find turnover positively related to future CSE returns (0.007 to 0.017, significant), which is the opposite sign to Baker and Stein's US prediction, and they find expected illiquidity negatively related to returns, flipping Amihud's original US result. Samarawickrama & Pallegedara (P13) independently read the CSE leverage effect as irrational investor behaviour. Meanwhile every LLM agent framework (P01, P02, P04) hard-codes the assumption that positive sentiment is bullish, because on their markets it is.|Existing approach: treat 'positive sentiment implies buy' as a universal. Limitation: P11 shows the direction of the sentiment-return relationship depends on the market's investor base, regulatory maturity and level of herd behaviour. On the CSE high turnover looks like attention and demand, not overvaluation due for correction.|This is the clearest case where the CSE doesn't just have less data than the US, it behaves differently. An agent that inherits a US-calibrated sentiment prior will systematically read CSE signals backwards.|Domain: empirical evidence on whether a text-derived sentiment signal on the CSE points the same direction as the turnover proxy, and whether either matches the developed-market prior. Technical: an agent architecture that learns the sign of the sentiment-return relationship from local data rather than assuming it.|Don't hard-code sentiment polarity into the trading rule. Let the RL agent learn the mapping from the sentiment feature to the action from CSE data, and report the learned relationship as a result in its own right. Run text sentiment and the turnover proxy side by side as separate state features so I can see whether they agree, and test the expected/unexpected decomposition on the text signal the way P11 does on illiquidity.|Sentiment analyst agent; RL state representation; research debate prompts (which must not assert a polarity prior).|CSE monthly and daily returns, turnover, Amihud illiquidity per ticker, plus the text sentiment series from G3. Ideally over a window covering all three regimes.|(a) US-prior hard-coded sentiment rule, which is the implicit baseline in P01/P02/P04. (b) Turnover-only, replicating P11 directly. (c) No sentiment feature at all.|Experiment: replicate P11's regression on my own sample as a sanity check, then substitute the text sentiment series for turnover and compare signs and significance. Ablation: text sentiment alone, turnover alone, both together, and split by market-cap tier since P11 finds small and large caps differ.|Nobody has put a text-derived sentiment signal next to the CSE liquidity literature, so whether they agree is an open empirical question.|4|High for the econometric part, since it's a direct replication and extension of P11 with data I can get. Dependent on G3 for the text signal.|If the text sentiment series is noisy - which the ~66% accuracy ceiling suggests it might be - the regression may be inconclusive rather than informative. That's still a reportable result, but it's not the headline I'd want. Mitigation: report it with confidence intervals and be honest about the signal-to-noise, rather than over-claiming from a weak coefficient.|ACCEPT - supporting contribution|
|G6|Agentic trading systems can't explain themselves in any way a regulator or an investor would accept, and the field knows it.|Technical|Pippas et al. (P05) flag interpretability as a first-order problem in finance because stakeholders have to understand and validate a recommendation before acting on it, and then the survey documents a literature of neural policies that can't. Kashif & Slepaczuk (P15) is a pure neural policy with no attribution. TradingAgents (P01), FinVision (P02) and AlphaAgents (P04) all produce natural-language rationales, which is better, but an LLM's stated reason isn't evidence that it's the actual cause of the decision. Only Rizvi et al. (P10) and Aldasoro et al. (P14) use formal attribution methods (SHAP/LIME and SHAP respectively), and neither is inside a trading system. Note P14 is still on my not-yet-reviewed list, so treat that half of the evidence as provisional until I've actually read it.|Existing approach: either a black-box policy with no explanation, or an LLM narrating its own reasoning. Limitation: the first gives you nothing and the second gives you something that sounds like an explanation but isn't verifiable. AlphaAgents at least traces its RAG with an observability platform, which is the right instinct.|Less CSE-specific than the others, but it matters here because the market is small enough that any deployed system would face direct scrutiny, and because the sentiment component is operating at a low accuracy ceiling - which makes knowing why it said something more important, not less.|Technical: a layered explainability design where each agent's contribution is attributable by an appropriate method (LIME on the sentiment text, SHAP on the risk features, interpretable econometric parameters on the volatility layer, citation-enforced evidence on the fundamental layer, and the debate transcript over the top) rather than one post-hoc method bolted onto the whole system.|Per-component attribution rather than a single global method: LIME for sentiment token attribution (P10 shows it beats SHAP on short code-mixed text and costs less), SHAP for the risk agent's feature contributions (provisionally following P14, pending my own read of it), directly interpretable FIGARCH/GJR-GARCH parameters for the volatility layer, evidence strings with page numbers for the fundamental agent (following P06), and the bull/bear debate transcript as the decision-level record. RAG traced with an observability tool from the start, as in P04, rather than added later.|Every agent, plus the orchestration layer.|No additional data - this is an instrumentation requirement on the components built for G1-G4.|(a) The system with explanations stripped, to confirm the explainability layer doesn't cost accuracy. (b) LLM-narrated rationale only, matching P01/P02/P04.|Experiment: check whether attributions are faithful - do the features SHAP/LIME names as decisive actually change the decision when perturbed? Ablation: measure any accuracy or latency cost of running the attribution layer, since P10 notes SHAP's compute cost is a real constraint for anything near real time.|Layered per-component attribution across a hybrid LLM-plus-econometric-plus-RL trading system. The individual methods are standard; composing them so every stage of a trade decision is separately auditable is not.|3|High. All the methods are off-the-shelf. The work is design and instrumentation discipline rather than novel research.|Risk of this becoming a checkbox rather than a contribution. Attribution methods can also be inconsistent themselves, and P10 found both SHAP and LIME get less reliable on low-resource code-mixed input - so the explanations for the sentiment agent are exactly the ones least likely to be trustworthy. Worth stating plainly rather than glossing over.|ACCEPT - supporting contribution|
|G7|Evaluation discipline. The multi-agent trading results I'm building on are reported on three tickers over three months with no significance testing, and there's no CSE benchmark to compare anything against.|Evaluation|TradingAgents (P01) reports Sharpe 8.21 on three megacap US tickers over a three-month window. FinVision (P02) uses three tickers over nine months. AlphaAgents (P04) uses 15 stocks in one sector over four months and reports no headline number at all. Against that, Kashif & Slepaczuk (P15) run 16 walk-forward folds across three markets over 23 years with HAC-adjusted and bootstrap tests - and find no statistically significant mean-return outperformance over buy-and-hold anywhere, despite real IR2 differences.|Existing approach: short backtest, headline cumulative return and Sharpe, no significance test, no walk-forward. Limitation: P15 is the proof of what happens when you do test properly - the economic metric and the statistical test disagree, and only the honest setup surfaces that.|CSE has fewer liquid names and a shorter reliable history than any of these markets, so the temptation to report a short flattering backtest is stronger and the risk of overfitting is higher. There's also no published CSE trading benchmark to compare against, so I have to build the comparison set myself.|Domain: a properly evaluated baseline set for automated trading on the CSE, including the walk-forward and significance machinery, which doesn't currently exist and would be reusable by anyone working on this market afterwards.|Non-anchored walk-forward optimisation following P15's scheme, adapted to the shorter CSE history. Report IR2 (which penalises drawdown directly) alongside Sharpe, and always next to a HAC-adjusted or bootstrap significance test. Decompose results across the normal / COVID / crisis regimes from P12 rather than pooling, since P15 shows regime decomposition is where the interesting behaviour lives. Report the fundamental agent on the correct / incorrect / failure-to-answer taxonomy from P06 rather than plain accuracy.|Whole framework - this is the evaluation protocol, not a component.|The full CSE price and turnover history I can obtain, which is the binding constraint. Historical depth on the CSE API is currently unconfirmed and needs probing before the walk-forward scheme can be designed.|Buy-and-hold on ASPI, buy-and-hold on S&P SL20, equal-weight monthly on a momentum-filtered universe, Markowitz minimum variance, and a single-agent LLM trader. The equal-weight baseline matters most: P15 shows it beats RL in two of three markets, so if my framework can't beat 1/N it doesn't have a result.|Experiment: every claim reported with the economic metric and the significance test side by side, decomposed by regime. Ablation: this is the protocol under which every other ablation (G1-G6) runs, so the contribution is that the ablations are trustworthy rather than that this produces a number of its own.|Not novel as a method - it's standard practice that the multi-agent trading literature specifically isn't following. The novelty is the CSE baseline set it produces.|2|High methodologically, but gated on data availability. If CSE historical depth turns out to be short, the walk-forward fold structure has to shrink and statistical power drops with it.|Insufficient historical depth is the single biggest threat to the whole project, not just this gap - if I can't get enough CSE history the RL component has no training data and the design has to change. This needs answering in week one. Secondary risk: with a small universe and short history, honest significance testing may well return 'no significant outperformance', exactly as it did for P15. I should decide now that reporting that is an acceptable outcome.|ACCEPT - evaluation protocol, gates everything else|